<div class="container mt-5">
    <h1 class="mb-4">Shopping Cart</h1>

    {{#if result}}
        <div class="card">
            <div class="card-body">
                <p class="card-text"><strong>Cart ID:</strong> {{result._id}}</p>

                <ul class="list-group">
                    {{#each result.products }}
                        <li class="list-group-item">
                            <h3 class="mb-2">Title: {{this._id.title}}</h3>
                            <p class="mb-1">Description: {{this._id.description}}</p>
                            <p class="mb-1">Category: {{this._id.category}}</p>
                            <p class="mb-1">Quantity: {{this.quantity}}</p>
                        </li>
                    {{/each}}
                </ul>
            </div>
        </div>
    {{else}}
        <div class="alert alert-warning" role="alert">
            <h4 class="alert-heading">Empty Cart</h4>
            <p>{{message}}</p>
        </div>
    {{/if}}
</div>
